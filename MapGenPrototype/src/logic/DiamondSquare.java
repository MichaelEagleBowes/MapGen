package logic;

/**
 * 
 * Generates a map on construction and saves the map as file. Can read map files
 * in and draw the read-in map file.
 * 
 */

public class DiamondSquare {
	int mapSize = (int) Math.pow(2, 2) + 1; // size of the map

}